# Database Configuration
DATABASE_URL=postgresql://repensar:repensar_password@localhost:5432/repensar_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=repensar
DB_PASSWORD=repensar_password
DB_NAME=repensar_db

# Security
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=30

# Email Configuration (SMTP)
# Leave empty to disable email sending (emails will be logged instead)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=labsfoss@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=labsfoss@repensar.com
EMAIL_FROM_NAME=Repensar
ADMIN_NOTIFICATION_EMAIL=labsfoss@gmail.com


# For Gmail SMTP:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the app password as SMTP_PASSWORD
#
# For other providers:
# - SendGrid: smtp.sendgrid.net:587
# - Mailgun: smtp.mailgun.org:587
# - AWS SES: email-smtp.us-east-1.amazonaws.com:587

# Application URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

# Google OAuth Configuration (for Google Sign In)
# Leave empty to disable Google Sign In
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=430386236663-vahk2a55qbccahd0n9cc2b05j1fe86st.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-seret-key
GOOGLE_REDIRECT_URI=http://localhost:3000/en/auth/google/callback
# Setup Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API (or Google Identity)
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add authorized redirect URI: http://localhost:8000/auth/google/callback
# 6. Copy Client ID and Client Secret above
# 7. For production, update GOOGLE_REDIRECT_URI to your production domain

# Redis (Optional - for production token blacklist & rate limiting)
# REDIS_URL=redis://localhost:6379/0

# Development
DEBUG=true
DISABLE_RATE_LIMITING=true

# PRODUCTION
DEBUG=false
DISABLE_RATE_LIMITING=false
